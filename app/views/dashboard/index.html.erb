<div class="row">
	<% @usertrait.each do |n| %>
		<div class ="large-4 columns" id="trait_list">
			<%= link_to "#{n.name} #{Trait.find(n.id).trait_user.first.counter} Times" , counter_index_path(:trait_id => n), method: :POST %>
		</div>
	<% end %>
</div>

<br>
<br>
<row>
	<div class ="large-3 columns">
	</div>
<%= form_for :trait, :url=>traits_path, :method=> :POST, html: {id: "add_trait_form"} do |f| %>
  Add a Trait: <%= f.text_field :name, :placeholder => "Add something you do here!", class:"form_text_field" %>
<%= f.submit 'Add it!', class: "formbutton", id: "formid" %>
  <% end %>
