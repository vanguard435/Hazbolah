$(document).ready(function(){
  setTimeout(function(){
    $('.kill').fadeOut("slow");
  }, 2000);
  	$('.trait_list').each(function(){
  		$(this).click(function(e){
  		if(e.target === this) {
  	  $(this).find(".fi-prohibited").fadeToggle();
  			}
  		})
  	})
  // $(document).on("ajax:complete", ".replacemiddle",function(xhr, data, status){
  //  console.log(data);
  //     $(".wrapper_trait").replaceWith(data);
//   });

//$("a.replacemiddle").bind("ajax:complete", function(evt, data, status, xhr){
//$.getJSON(data, function(data){
 // for(var i = 0 , i < data.length; i++){
//    console.log(data[i])
//  )};
//}
 

  })

